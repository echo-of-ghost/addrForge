# addrForge
Bitcoin vanity address generator. TUI and CLI. All four address types (Taproot, Native SegWit, Nested SegWit, Legacy), prefix/suffix/regex matching, MuSig2 key aggregation, and address inspector. Single binary, no dependencies at runtime.
